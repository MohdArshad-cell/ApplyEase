# ===================================================================
# Local Development Profile (dev)
# ===================================================================

# Server
server.port=8080

# Local MySQL Database Connection
spring.datasource.url=jdbc:mysql://localhost:3306/aplyease_db?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=p0st25arr

# JPA / Hibernate Settings for MySQL
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Logging
logging.level.org.springframework.security=DEBUG

# JWT Secret - Uses an environment variable if present, otherwise falls back to a default for easy local startup.
# JWT Secret - Uses an environment variable if present, otherwise falls back to a default for easy local startup.
app.jwt.secret=${JWT_SECRET:bXlzdXBlcnNlY3JldGtleWZvcmFwbHllYXNlYW5kaXRzaG91bGRiZWxvbmdhbmRzZWN1cmU=}